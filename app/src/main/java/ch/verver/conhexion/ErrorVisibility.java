package ch.verver.conhexion;

import androidx.annotation.Nullable;

/**
 * Specifies whether overlap errors are displayed or not.
 *
 * <p>The names of enumerators are stored in SharedPreferences, so these should not be changed
 * unnecessarily!
 */
enum ErrorVisibility {
    HIDDEN, VISIBLE
}
